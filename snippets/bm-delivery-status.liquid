{%- comment -%}
  Deprecated: delivery status is now handled by the theme's native
  inventory block + product-deliverytime snippet. Positioned via
  block_order in templates/product.json (inventory before buy_button).
  The theme's variant change system (section re-rendering) automatically
  updates .f8pr-stock on variant selection.
{%- endcomment -%}
